# Laravel
alias laravel='~/.config/composer/vendor/bin/laravel'

# Custom rails command
alias rails-kill='kill -9 $(lsof -ti tcp:3000)'

# Monkey patch oh-my-zsh alias
alias gp='ggpush'

# Sweevr
alias sw='cd ~/sweevr'

# tmux
alias tmux='env TERM=screen-256color tmux'
alias mux='tmuxinator'

# Others
alias zsh-reload=". ~/.zshrc && echo 'ZSH config reloaded from ~/.zshrc'"
alias uct='ctags -R --exclude=node_modules .'
alias jest-debug='node --inspect node_modules/.bin/jest --runInBand'

# WORK
# Tanda
alias tanda-remote-ssh='ssh ruby@3.105.74.199'
alias tanda-sync='cd ~/tanda/payaus && ./useful_scripts/dev-server/sync-aws.sh kojidelmundo'
alias tanda='cd ~/tanda/payaus'
alias ssh-reset='ssh-keygen -R 3.105.74.199'
alias tamux='tanda && tmux'
alias ts='tanda-remote-ssh'

# Mashup Garage
alias skippr='cd ~/skippr && mux skippr'
